# Agent (L1) Requirements

# Windows Service Support
pywin32>=305

# Database (SQLite is built-in, but may need additional tools)
# sqlite3 is built-in

# File System Monitoring
# Uses Windows API via ctypes (built-in)

# Network Monitoring
# Uses socket (built-in)

# Process Monitoring
# Uses Windows API via ctypes (built-in)

# USB Monitoring
# Uses Windows API via ctypes (built-in)

# Clipboard Monitoring
# Uses Windows API via ctypes (built-in)

# Context/Window Title
# Uses Windows API via ctypes (built-in)

# HTTP client (agent_sender → central server)
httpx>=0.27.0

# GUI & System Tray
pystray>=0.19.5
Pillow>=9.0.0
